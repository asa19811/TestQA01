 1 #Область ПрограммныйИнтерфейс
 2
 3 //Это комментарий к процедуре на двух строках
 4 //Чтобы посмотреть цвет выделения, выделите текст
 5 &НаКлиенте
 6 Процедура СделатьХорошо(Массив) Экспорт
 7     
 8     ОтветНаГлавныйВопрос = 42; //Текущая строка
 9     ПростоСтрока = "Просто строка";
10     ДеньПрограммиста = '20200912';
11     
12     Для Индекс = 0 По 1023 Цикл //Выбранный идентификатор
13         ТекущийОбъект = Массив[Индекс];
14         Если ЗначениеЗаполнено(ТекущийОбъект) Тогда
15             ТекущийОбъект.ДелаемХорошо();
16         Иначе
17             Сообщить("Не заполнен элемент # " + Индекс);
18         КонецЕсли;
19     КонецЦикла;
20     
21 КонецПроцедуры
22
23 #КонецОбласти
